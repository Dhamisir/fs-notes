const form=document.querySelector("#search-form"),queryInput=document.querySelector("#query"),repeatButton=document.querySelector("#repeat"),statusText=document.querySelector("#status"),responseCode=document.querySelector("#response-code"),completedText=document.querySelector("#completed"),processedText=document.querySelector("#processed"),results=document.querySelector("#results");
const terms=["keyboard","monitor","camera","charger","speaker","mouse","headphones","portable"];
async function search(query=queryInput.value){const response=await fetch("/api/products/search?q="+encodeURIComponent(query));responseCode.textContent=response.status;const data=await response.json();if(typeof data.processedSearches==="number")processedText.textContent=data.processedSearches;if(Array.isArray(data.results))results.innerHTML=data.results.map((p)=>"<article><strong>"+p.name+"</strong><span>"+p.category+" · $"+p.price+" · "+p.quantity+" available</span></article>").join("");return response.status;}
form.addEventListener("submit",async(e)=>{e.preventDefault();statusText.textContent="Searching…";try{await search();statusText.textContent="Search complete";}catch{statusText.textContent="Request failed";}});
repeatButton.addEventListener("click",async()=>{repeatButton.disabled=true;statusText.textContent="Sending varied repeated requests…";let completed=0;try{const requests=Array.from({length:30},(_,i)=>search(terms[i%terms.length]).then((status)=>{completed+=1;completedText.textContent=completed;return status;}));const statuses=await Promise.all(requests);statusText.textContent="Finished: "+statuses.length+" responses received";}catch{statusText.textContent="Some requests failed";}finally{repeatButton.disabled=false;}});
